@* <div id="eachEmployeeContent">
    <h3>Each Employee</h3>
    <a id="salarySlipLink" class="btn btn-primary" asp-controller="Report" asp-action="EachEmployeeGrid">Salary Slip</a>
    <a class="btn btn-primary" asp-controller="Report" asp-action="ProfessionalTax">Professional Tax Deduction Report</a>
</div>
<div id="eachEmployeeDynamicContent"></div> *@


        <div class="card-header d-flex justify-content-between">
            <h1>Each Employee</h1>
            <div class="button-group">
                <a id="salarySlipLink" class="btn btn-primary" asp-controller="Report" asp-action="EachEmployeeGrid">Salary Slip</a>
            </div>
        </div>
  
        <div id="eachEmployeeDynamicContent" class="card-body">
            <!-- Content for each employee goes here -->
        </div>
    


<style>
    /* Ensure the body and HTML have no padding or margin */
   /*  html, body {
        margin: 0;
        padding: 0;
        width: 100%;
        height: 100%;
        overflow-x: hidden; /* Prevent horizontal scrolling */
    } */

    /* .container {
        padding: 20px;
    } */

    .card-header {
        display: flex;
        justify-content: space-between; /* Align items to the left and right */
        align-items: center; /* Center vertically */
        padding: 10px 15px;
    }

    .card {
        background-color: #faf8f8;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        padding: 20px;
        width: 100%; /* Ensure card takes full width */
        max-width: 100%; /* Ensure card does not exceed container width */
        height: 100%; /* Ensure card takes full height */
        overflow-y: auto; /* Add vertical scroll if content exceeds height */
    }

    .button-group {
        display: flex;
        gap: 10px; /* Adjust spacing between buttons */
    }

        .button-group a {
            padding: 10px 20px;
            border: none;
            background-color: #007bff;
            color: #fff;
            border-radius: 5px;
            text-decoration: none; /* Remove underline from links */
            cursor: pointer;
        }


    #eachEmployeeContent {
        display: flex;
        align-items: center; /* Align items vertically */
        gap: 10px; /* Spacing between heading and buttons */
    }

   
</style>

<script>
    $(document).ready(function () {
        initialLoad();
    });

    function initialLoad(){
    document.getElementById('salarySlipLink').click();
    };
</script>

